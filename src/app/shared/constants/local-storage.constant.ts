/**
* @WhatItDoes define constants for variables stored in local storage
* @Author LDThien
* @Date 2018/02/24
*/
export const LOCAL_STORAGE = Object.freeze({
    TOKENS: 'tokens',
    CURRENT_USER: 'current_user',
});

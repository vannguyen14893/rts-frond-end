import { UserSearchBoxDirective } from './user-search-box.directive';

describe('UserSearchBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new UserSearchBoxDirective();
    expect(directive).toBeTruthy();
  });
});

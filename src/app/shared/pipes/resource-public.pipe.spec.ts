import { ResourcePublicPipe } from './resource-public.pipe';

describe('ResourcePublicPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourcePublicPipe();
    expect(pipe).toBeTruthy();
  });
});

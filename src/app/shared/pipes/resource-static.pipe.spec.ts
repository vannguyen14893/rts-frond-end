import { ResourceStaticPipe } from './resource-static.pipe';

describe('ResourceStaticPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourceStaticPipe();
    expect(pipe).toBeTruthy();
  });
});
